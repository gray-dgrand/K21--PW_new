# Lesson 01

- Khởi tạo dự án Playwright và cấu trúc test cơ bản.
- Sử dụng `test`, `expect`, `page.goto()`, `toHaveTitle()`.
- Tương tác phần tử bằng role/text và kiểm tra heading hiển thị đúng.
- Chạy test bằng `npx playwright test`.

